# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

This is the GitHub profile repository (`liyincode/liyincode`). It contains a single `README.md` that renders directly on https://github.com/liyincode. There is no code, build system, or tests.

## Key constraints

- Any commit pushed to `main` is immediately visible on the public GitHub profile page — treat every push as a production publish and confirm with the user before pushing.
- `README.md` content is intentionally curated (intro, tech stack icons, featured projects, open source contributions). Keep its existing structure and tone when editing; don't add sections unprompted.
- Tech stack icons use `<code><img>` tags pointing to `raw.githubusercontent.com/github/explore` assets — follow the same pattern when adding new ones.
